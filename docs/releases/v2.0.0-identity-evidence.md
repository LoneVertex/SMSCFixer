# SMSC Guard v2.0.0 Release Identity Evidence

## Intended release identity

| Field | Expected value |
|---|---|
| Public app/module name | SMSC Guard |
| Application ID and namespace | `io.github.lonevertex.smscguard` |
| Version name | `2.0.0` |
| Version code | `2000000` |
| Release tag | `v2.0.0` |
| Diagnostic tag | `SmscGuard` |
| Xposed entry class | `io.github.lonevertex.smscguard.SmscGuard` |
| Preferences name | `smscguard_prefs` |

## Local APK inspection

The rebuilt debug APK reported the expected `io.github.lonevertex.smscguard` package, version code `2000000`, version name `2.0.0`, application label **SMSC Guard**, and launcher activity `io.github.lonevertex.smscguard.SettingsActivity`. The APK contained the renamed `assets/xposed_init` entry. The instrumentation APK package was `io.github.lonevertex.smscguard.test`.

The manifest references both launcher and round icon resources. Android adaptive resources are defined for API 26+; density-specific fallback mipmaps are present for earlier launchers. The master icon and deterministic derivative generator are recorded in `docs/brand/smsc-guard-icon.md`.

## Candidate manifest evidence

`./scripts/verify_release_config.sh v2.0.0` passed. The unsigned release-candidate manifest generated from the local v2.0.0 build records the release tag, version name, version code, application ID, commit SHA, unsigned artifact filename, SHA-256 digest, external signing ownership, and generation time.

## Quality gate

The package identity and internal entry-class migration passed the local Java 17 / Android SDK 36 gate:

```text
./gradlew --no-daemon clean lint test assembleDebug assembleRelease assembleDebugAndroidTest
BUILD SUCCESSFUL
```

## Residual external gates

The local identity evidence does not prove device installation, LSPosed scope recognition, cross-process preference access, carrier delivery, signed-artifact verification, rollback, remote push, pull request, or release publication. Those actions remain controlled by the v2.0.0 migration guide, validation matrix, signing handoff verifier, and rollback evidence worksheet.
